# Retired ProtoKit Experiment Loop

**Status:** legacy migration pointer

Use the current [Experiment Workflow](how-to-experiment.md). It composes
NexusEngine Core with trusted registry kits and keeps complete game behavior in
an experiment or game repository.

The original loop is preserved unchanged at
[`legacy/protokits/protokit-experiment-loop.md`](legacy/protokits/protokit-experiment-loop.md).
