# Retired ProtoKit Boundaries

**Status:** legacy migration pointer

ProtoKits are not an active architecture destination. Current ownership is
defined by [Kit Ownership](KIT-OWNERSHIP.md).

The original boundary document is preserved unchanged at
[`legacy/protokits/protokit-boundaries.md`](legacy/protokits/protokit-boundaries.md).
