import { createDomainKit } from "../../../../../domain-kit.js";
import { assignAuthority, createAuthorityRecord } from "../../contracts.js";
export function createMultiplayerAuthorityKit(config = {}) { return createDomainKit({ ...config, manifestId: "multiplayer-authority-kit", id: config.id ?? "multiplayer-authority-kit", domain: "multiplayer-authority", domainPath: "n:network:multiplayer:authority", parentDomainPath: "n:network:multiplayer", apiName: "multiplayerAuthority", requires: ["n:network:multiplayer"], provides: ["n:network:multiplayer:authority", "multiplayer:authority"], initialState: { authority: createAuthorityRecord() }, purpose: "Single-host role and state ownership contracts.", owns: ["peer roles", "state ownership"], doesNotOwn: ["game rules", "transport election"], createApi() { return { createAuthorityRecord, assignAuthority }; } }); }
