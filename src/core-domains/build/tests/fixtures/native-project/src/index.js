export const nexusBuildMode = "native";

export function add(left, right) {
  return left + right;
}

export function scaledOffset(value, scale) {
  return value * scale + 2;
}

export function identifierCollision($value, _value) {
  return $value + _value;
}
