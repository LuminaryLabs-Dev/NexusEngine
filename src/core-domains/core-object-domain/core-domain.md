# Core Object Domain

The optional composite installs the universal Core Object identity contract together with Object Shape and Object Fidelity.

```txt
n:core-object
├─ n:object:shape
└─ n:object:fidelity
```

The composite adds no state of its own. Each child remains independently installable and snapshot-capable.
