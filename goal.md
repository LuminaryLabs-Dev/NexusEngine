# Active Goal

The canonical NexusEngine `0.0.5` goal, matrices, checklist, delivery queue,
readiness state, and event history are stored under:

`.agent/versions/0.0.5/`

Start with `.agent/versions/0.0.5/README.md`. The current bounded package is
Physics Detection. No push or release action is authorized.
